# Sensitive environment variables that should NOT leak into untrusted scripts.
# Shared between `safe_command::build_suggestion` (env-scrub transform) and
# later M9 ch4 env-guard rules.
sensitive = [
  "AWS_ACCESS_KEY_ID",
  "AWS_SECRET_ACCESS_KEY",
  "AWS_SESSION_TOKEN",
  "GITHUB_TOKEN",
  "GH_TOKEN",
  "NPM_TOKEN",
  "PYPI_TOKEN",
  "OPENAI_API_KEY",
  "ANTHROPIC_API_KEY",
  "STRIPE_API_KEY",
  "DOCKER_PASSWORD",
  "SLACK_TOKEN",
]
