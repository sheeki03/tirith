# Native byte growth qualification inputs

These twelve temporary source variants and this controller prepare six paired
resource-detector checks for the exact historical source `415565d3`. No mutation
belongs in the product branch. This controller adds qualification documents only;
it preserves the source collector, checker, workflow and measurement producers.

The protocol is a draft pending review of these exact source ports. The collector
refuses draft protocols before compilation. Nothing has been dispatched and no
budget is active. Final product source still requires three fresh baselines and
its own compatibility review before an enforced CI budget can be considered.

The separately retained threshold review approves the unchanged six byte ceilings
only for isolated growth qualification. The draft budget's numeric thresholds,
runner, toolchain, producers, helper and baseline identities are unchanged here.
The reviewed copy changes status/review metadata and replaces local evidence
locations with the matching GitHub run/artifact identities. Its prior draft remains
retained independently.

Each variant has exactly one parent, the protocol base. Its tree differs from that
base only in the declared product file. Ported added lines are byte-for-byte equal
to the previously reviewed mutation; selectors and doses have not been adapted.
The zero-dose variant retains the same branch/counter shape as its grown partner.
All real allocation variants touch each 4096-byte page and retain their buffer for
the selected analysis or history call. The ordinary-check RSS guard retains its
buffer until command return.

The frozen allocation producer calls clean analysis with `git status`, then URL
analysis with `curl https://example.invalid/install.sh | bash`, 100 times each.
Each selector increments its counter only after the exact command match. The
first/subsequent mutations therefore select sample 0 or samples 1 through 99.
The history selector matches `recent(..., 100, true)`. The RSS selector matches the
local producer's exact `echo fixture` ordinary check.

| Experiment | Dose in bytes | Product file |
| --- | ---: | --- |
| clean_first | 33,554,432 | crates/tirith-core/src/engine.rs |
| clean_subsequent | 32,768 | crates/tirith-core/src/engine.rs |
| url_first | 33,554,432 | crates/tirith-core/src/engine.rs |
| url_subsequent | 524,288 | crates/tirith-core/src/engine.rs |
| history_requests | 4,194,304 | crates/tirith-core/src/history.rs |
| ordinary_rss | 16,777,216 | crates/tirith/src/cli/check.rs |

After exact-protocol review, create a separate reviewed protocol commit, push all
thirteen refs without merging them, and dispatch the existing `bench.yml` once per
experiment at that immutable controller revision. Use the tracked protocol path,
`collect_pinned_reference=false`, and `resource_samples=100`. Each job serially
builds and measures its control and growth variants on one retained boot. The
workflow has a 120-minute job timeout and uploads collected evidence on failure.
No automatic retries or cohort relabeling are authorized by this protocol.

Both roles must match the actual `Apple M1 (Virtual)` CPU, macOS image/version,
Python version/executable bytes and Rust/Cargo contract in the budget. A failed
class admission remains a refusal. The old Linux URL-first qualification remains
historical; the five other old Linux CPU mismatches remain refusals.

The control must pass all six ceilings; growth must exceed exactly the selected
ceiling. Every active allocation delta must equal the fixed dose within 4096
bytes with an actual allocation call, and inactive samples must stay within
4096 bytes. RSS requires all 100 growth observations above the ceiling and median
paired growth of at least 75 percent of the 16 MiB dose. Distinct measured binary
identities, native boot equality within each pair and source before/after equality
are mandatory. Actual results, including failures, must be retained and reviewed.
